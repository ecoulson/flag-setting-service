import { Annotation } from 'noose-injection';

export const TimestampGeneratorAnnotation = new Annotation(
    'TimestampGenerator'
);
export const TimestampFormatterAnnotation = new Annotation(
    'TimestampFormatter'
);
